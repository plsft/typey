# Normalize all text files to LF in the repo regardless of contributor OS.
* text=auto eol=lf

# Lockfiles and binaries should not be touched.
bun.lock binary
*.png binary
*.jpg binary
*.gif binary
*.ico binary
